{{- $values := include "childcharts.tracing.byconity.values" . | fromYaml -}}
{{- $ctxValues := mustMergeOverwrite (dict "global" ((index .Values.tracing "global") | default dict)) (deepCopy $values) -}}
{{- $ctx := dict "Values" $ctxValues "Release" .Release "Chart" .Chart "Capabilities" .Capabilities "Files" .Files "Template" .Template -}}
{{- if and .Values.tracing.enabled ((index .Values.tracing "byconity" "enabled") | default false) -}}
{{- with $ctx -}}
{{- $ctxRoot := . -}}
{{- $imageRepository := tpl $ctxRoot.Values.image.repository . }}
{{- with .Values.byconity.tso }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "childcharts.tracing.byconity.fullname" $ }}-tso
  labels:
    {{- include "childcharts.tracing.byconity.labels" $ | nindent 4 }}
    byconity-role: tso
spec:
  replicas: {{ .replicas }}
  serviceName: {{ include "childcharts.tracing.byconity.fullname" $ }}-tso
  selector:
    matchLabels:
      {{- include "childcharts.tracing.byconity.selectorLabels" $ | nindent 6 }}
      byconity-role: tso
  updateStrategy:
    type: RollingUpdate
  template:
    metadata:
      {{- with .podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "childcharts.tracing.byconity.selectorLabels" $ | nindent 8 }}
        byconity-role: tso
    spec:
      containers:
        - name: byconity-tso
          image: "{{ $imageRepository }}:{{ $ctxRoot.Values.image.tag }}"
          imagePullPolicy: {{ $ctxRoot.Values.image.imagePullPolicy }}
          env:
            {{- toYaml $ctxRoot.Values.byconity.commonEnvs | nindent 12 }}
            - name: BYCONITY_ROLE
              value: tso
          {{- if $ctxRoot.Values.byconity.additionalEnvs }}
            {{- toYaml $ctxRoot.Values.byconity.additionalEnvs | nindent 12 }}
          {{- end }}
            - name: TSO_IP
              valueFrom:
                fieldRef:
                  fieldPath: "status.podIP"
          {{- if .resources }}
          resources:
            {{- toYaml .resources | nindent 12 }}
          {{- end }}
          ports:
            - name: tcp
              containerPort: {{ $ctxRoot.Values.byconity.ports.tcp }}
          readinessProbe:
            {{- .readinessProbe | toYaml | nindent 12 }}
          livenessProbe:
            {{- .livenessProbe | toYaml | nindent 12 }}
          lifecycle:
            postStart:
              exec:
                command: ["/opt/byconity/scripts/lifecycle/poststart"]
            preStop:
              exec:
                command: ["/opt/byconity/scripts/lifecycle/prestop"]
          volumeMounts:
            - name: run
              mountPath: /etc/service/byconity/run
              subPath: run
            - name: config
              mountPath: /etc/byconity
            - name: lifecycle
              mountPath: /opt/byconity/scripts/lifecycle
            - name: fdb-config
              mountPath: /etc/byconity/fdb
            - name: local-disk
              mountPath: /var/byconity
            - name: log
              mountPath: /var/log/byconity
          {{- if $ctxRoot.Values.byconity.tso.additionalVolumes.volumeMounts }}
            {{- toYaml $ctxRoot.Values.byconity.tso.additionalVolumes.volumeMounts | nindent 12 }}
          {{- end }}
          {{- if .securityContext }}
          securityContext:
            {{- toYaml .securityContext | nindent 12 }}
          {{- end }}
      volumes:
        - name: run
          configMap:
            name: {{ include "childcharts.tracing.byconity.fullname" $ }}-tso-run
            defaultMode: 0755
        - name: config
          configMap:
            name: {{ include "childcharts.tracing.byconity.fullname" $ }}-tso-config
        - name: lifecycle
          configMap:
            name: {{ include "childcharts.tracing.byconity.fullname" $ }}-lifecycle
            defaultMode: 0755
        - name: fdb-config
          configMap:
            name: {{ include "childcharts.tracing.byconity.fullname" $ }}-fdb-config
      {{- if $ctxRoot.Values.byconity.tso.additionalVolumes.volumes }}
        {{- toYaml $ctxRoot.Values.byconity.tso.additionalVolumes.volumes | nindent 8 }}
      {{- end }}
      hostNetwork: {{ .hostNetwork }}
      {{- with .nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      imagePullSecrets:
      {{- with $ctxRoot.Values.global.imagePullSecrets }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $ctxRoot.Values.imagePullSecrets }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
  volumeClaimTemplates:
    - metadata:
        name: local-disk
      spec:
        {{- include "childcharts.tracing.byconity.pvcSpec" (dict "spec" .storage.localDisk.pvcSpec "global" $ctxRoot.Values.global) | nindent 8 }}
    - metadata:
        name: log
      spec:
        {{- include "childcharts.tracing.byconity.pvcSpec" (dict "spec" .storage.log.pvcSpec "global" $ctxRoot.Values.global) | nindent 8 }}
    {{- if $ctxRoot.Values.byconity.tso.additionalVolumeClaimTemplates }}
    {{- toYaml $ctxRoot.Values.byconity.tso.additionalVolumeClaimTemplates | nindent 4 }}
    {{- end }}
{{- end }}

{{- end -}}
{{- end -}}
