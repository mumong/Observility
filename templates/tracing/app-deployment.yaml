apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "deepflow.fullname" . }}-app
  labels:
    {{- include "deepflow-app.labels" . | nindent 4 }}
spec:
  replicas: {{ tpl (toString .Values.tracing.app.replicas) . }}
  selector:
    matchLabels:
      {{- include "deepflow-app.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        {{- with .Values.tracing.app.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        checksum/config: {{ include "deepflow.configmap.checksum" . }}
        checksum/customConfig: {{ sha256sum (print (tpl (toYaml .Values.tracing.configmap) $)) }}
      labels:
        {{- include "deepflow-app.selectorLabels" . | nindent 8 }}
        {{- with .Values.tracing.app.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      hostNetwork: {{ tpl .Values.tracing.app.hostNetwork . }}
      dnsPolicy: {{ tpl .Values.tracing.app.dnsPolicy . }}
      imagePullSecrets:
      {{- with .Values.tracing.global.imagePullSecrets }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tracing.imagePullSecrets }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.tracing.podSecurityContext | nindent 8 }}
      containers:
        - name: deepflow-app
          securityContext:
            {{- toYaml .Values.tracing.securityContext | nindent 12 }}
          image: "{{ tpl .Values.tracing.image.app.repository . }}:{{ tpl .Values.tracing.image.app.tag . }}"
          imagePullPolicy: "{{ tpl .Values.tracing.image.app.pullPolicy . }}"
          ports:
            - name: app
              containerPort: 20418
              protocol: TCP
          livenessProbe:
            {{ toYaml .Values.tracing.app.livenessProbe | nindent 12 }}
          readinessProbe:
            {{ toYaml .Values.tracing.app.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.tracing.app.resources | nindent 12 }}
          volumeMounts:
          - name: app-config
            mountPath: /etc/deepflow/app.yaml
            subPath: app.yaml
          env:
          - name: K8S_NODE_IP_FOR_DEEPFLOW
            valueFrom:
              fieldRef:
                fieldPath: status.hostIP
          - name: K8S_NODE_NAME_FOR_DEEPFLOW
            valueFrom:
              fieldRef:
                fieldPath: spec.nodeName
          - name: K8S_POD_NAME_FOR_DEEPFLOW
            valueFrom:
              fieldRef:
                fieldPath: metadata.name
          - name: K8S_POD_IP_FOR_DEEPFLOW
            valueFrom:
              fieldRef:
                fieldPath: status.podIP
          - name: TZ
            value: "{{ tpl .Values.tracing.timezone . }}"
      volumes:
        - name: app-config
          configMap:
            name: {{ include "deepflow.fullname" . }}
            items:
            - key: app.yaml
              path: app.yaml
      {{- with .Values.tracing.app.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      affinity:
      {{- include "appNodeaffinity" . | indent 6 }}
      {{- include "appPodAffinity" . | indent 6 }}
      {{- include "appPodAntiAffinity" . | indent 6 }}
      {{- if or .Values.tracing.global.tolerations .Values.tracing.tolerations }}
      tolerations:
      {{- if .Values.tracing.global.tolerations }}
      {{- toYaml .Values.tracing.global.tolerations | nindent 8 }}
      {{- end }}
      {{- if .Values.tracing.tolerations }}
      {{- toYaml .Values.tracing.tolerations | nindent 8 }}
      {{- end }}
      {{- end }}
