{{- if and .Values.metric.enabled .Values.metric.kepler.enabled .Values.metric.kepler.modelServer.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "modelServer.fullname" . }}
  labels:
    {{- include "modelServer.labels" . | nindent 4 }}
  {{- with .Values.metric.kepler.modelServer.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.metric.kepler.modelServer.replicas }}
  selector:
    matchLabels:
      {{- include "modelServer.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.metric.kepler.modelServer.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "modelServer.selectorLabels" . | nindent 8 }}
        {{- with .Values.metric.kepler.modelServer.podLabels }}
        {{- . | toYaml | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.metric.kepler.modelServer.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: server-api
        args:
        - model-server
        image: "{{ .Values.metric.kepler.modelServer.image.repository }}:{{ .Values.metric.kepler.modelServer.image.tag }}"
        imagePullPolicy: {{ .Values.metric.kepler.modelServer.image.pullPolicy }}
        ports:
        - containerPort: 8100
          name: http
          protocol: TCP
        volumeMounts:
        - mountPath: /mnt
          name: mnt
        {{- with .Values.metric.kepler.modelServer.resources }}
        resources:
          {{- toYaml . | nindent 12 }}
        {{- end }}
        {{- with .Values.metric.kepler.modelServer.securityContext }}
        securityContext:
          {{- toYaml . | nindent 12 }}
        {{- end }}
        startupProbe:
          httpGet:
            path: /best-models
            port: http
          initialDelaySeconds: 1
        readinessProbe:
          httpGet:
            path: /best-models
            port: http
      volumes:
      - name: mnt
        emptyDir: {}
      {{- with .Values.metric.kepler.modelServer.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.metric.kepler.modelServer.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.metric.kepler.modelServer.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.metric.kepler.modelServer.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
