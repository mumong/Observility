{{- if and .Values.metric.enabled .Values.metric.kepler.enabled .Values.metric.kepler.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "kepler.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels: 
    {{- include "kepler.labels" . | nindent 4 }}
  {{- with .Values.metric.kepler.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.metric.kepler.networkPolicy.ingress }}
  ingress:
      {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.metric.kepler.networkPolicy.egress }}
    egress:
      {{- toYaml . | nindent 4 }}
  {{- end }}
  podSelector:
    matchLabels:
      {{- include "kepler.selectorLabels" . | nindent 6 }}
  policyTypes:
  - Egress
  - Ingress
{{- end }}
