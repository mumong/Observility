{{- if and .Values.metric.enabled .Values.metric.others.kubeProxy.enabled .Values.metric.others.kubeProxy.service.enabled .Values.metric.others.kubernetesServiceMonitors.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ template "kube-prometheus-stack.fullname" . }}-kube-proxy
  labels:
    app: {{ template "kube-prometheus-stack.name" . }}-kube-proxy
    jobLabel: kube-proxy
{{ include "kube-prometheus-stack.labels" . | indent 4 }}
  namespace: kube-system
spec:
  clusterIP: None
  {{- if .Values.metric.others.kubeProxy.service.ipDualStack.enabled }}
  ipFamilies: {{ toYaml .Values.metric.others.kubeProxy.service.ipDualStack.ipFamilies | nindent 4 }}
  ipFamilyPolicy: {{ .Values.metric.others.kubeProxy.service.ipDualStack.ipFamilyPolicy }}
  {{- end }}
  ports:
    - name: {{ .Values.metric.others.kubeProxy.serviceMonitor.port }}
      port: {{ .Values.metric.others.kubeProxy.service.port }}
      protocol: TCP
      targetPort: {{ .Values.metric.others.kubeProxy.service.targetPort }}
{{- if .Values.metric.others.kubeProxy.endpoints }}{{- else }}
  selector:
    {{- if .Values.metric.others.kubeProxy.service.selector }}
{{ toYaml .Values.metric.others.kubeProxy.service.selector | indent 4 }}
    {{- else}}
    k8s-app: kube-proxy
    {{- end}}
{{- end }}
  type: ClusterIP
{{- end -}}
